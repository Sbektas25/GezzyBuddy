name: gezzy_buddy_test
description: Test configuration for GezzyBuddy
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  gezzy_buddy:
    path: ..

flutter:
  uses-material-design: true
  
  assets:
    - test/assets/ 